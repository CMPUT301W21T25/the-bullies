<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cmput301w21t25">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CMPUT301W21T25">
        <activity android:name=".activities.OtherUserProfileActivity"></activity>
        <activity android:name=".activities.CreateExperimentActivity"></activity>
        <activity android:name=".activities.TempRightActivity" />
        <activity android:name=".activities.GenerateUserActivity" />
        <activity android:name=".activities.HomeSubbedActivity" />
        <activity android:name=".activities.SearchActivity" />
        <activity android:name=".activities.MyUserProfileActivity" />
        <activity android:name=".activities.HomeOwnedActivity" />
        <activity android:name=".activities.ViewExperimentActivity" />
        <activity android:name=".activities.ViewTrialActivity" />
        <activity android:name=".activities.ExperimentDataActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>