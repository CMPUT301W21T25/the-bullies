<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cmput301w21t25">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CMPUT301W21T25">
        <activity android:name=".OtherUserProfileActivity"></activity>
        <activity android:name=".CreateExperimentActivity"></activity>
        <activity android:name=".TempRightActivity" />
        <activity android:name=".GenerateUserActivity" />
        <activity android:name=".HomeSubbedActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".MyUserProfileActivity" />
        <activity android:name=".HomeOwnedActivity" />
        <activity android:name=".ViewExperimentActivity" />
        <activity android:name=".InspectTrialActivity" />
        <activity android:name=".ExperimentDataActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>